[package]
name = "just-gl"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.4.11", features = ["derive"] }
drm = "0.9"
gbm = { version = "0.12", default-features = false, features = ["import-egl", "drm-support"] }
